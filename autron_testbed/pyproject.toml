[project]
name = "autron-testbed"
version = "0.1.0"
description = "Local-only validation project for Autron integration progress"
requires-python = ">=3.10,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
